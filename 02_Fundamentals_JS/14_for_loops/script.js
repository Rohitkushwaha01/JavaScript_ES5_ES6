// Loop : A peices of code which repeat itself uptill certain conditions met.

// for loop : keeps running while conditions is true and stops if the condition becomes false.


for (let i = 1; i <= 30; i++) {
    // console.log("lifting weights " + i);
    console.log(`lifting weights ${i} `);
}

