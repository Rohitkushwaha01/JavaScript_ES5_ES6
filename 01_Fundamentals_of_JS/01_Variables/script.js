// variables = A container to store data in memory.

// In javascript the variables are in camelCase.

// Rules to declare a variable
// 1) Variable cannot start with a Number(e.g = 3items --> Not valid).
// 2) Variable should start with alphabets, underscore(_), dollarSign($).
// 3) Variable can contain alphabets, underscore(_), dollarSign($) and numbers.
// 4) Variable name cannot be a reserved keyword.
// 5) use Descriptive varaible (i.e variable should tell some info like age, firstName, lastName).



let greet =  "Hello Javascript!";


let myCountry = "India";
console.log(myCountry);

let _continent = "Asia";
console.log(_continent);

let $state = "Maharashtra";
console.log($state);

// let 3star = "3 start" // invalid (thows an error)
// let var = 4; // invalid 
// let d45d = 4 // valid but not understable(use descriptive variable)

