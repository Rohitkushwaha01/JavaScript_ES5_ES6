// Decision statement apply some condition on our code whether one part is executed or the another part.

const age = 19;
const isOldEnough = age >= 18; 

if (isOldEnough) {
    console.log("Rohit, you can drive!!");
}
else{
    console.log("Rohit, you cannot drive!!");
}